# A Story — website

React 19 + Vite + TypeScript. 20 prerendered routes, Supabase-backed waitlist.

```
npm ci
npm run dev          # local
npm run lint
npm run build:static # production build + prerender
```

Start with **[MAP.md](MAP.md)** — what every live file does and where the facts live.

House style: **[docs/code-conventions.md](docs/code-conventions.md)** — the original
author's conventions, and the standard for new work.

Mission and voice: **[docs/mission.md](docs/mission.md)** — the source of truth for content.

Redesign brief: **[docs/design-brief.md](docs/design-brief.md)**.

`src/` holds the original codebase plus the current marketing pages; MAP.md separates
them. `docs/history/` holds superseded planning documents.
