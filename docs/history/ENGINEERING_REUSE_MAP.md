# Engineering reuse

Retained because these are working contracts, not visual decisions:

- React, React Router, Vite, TypeScript, styled-components; unchanged package and lockfile.
- Pricing and product facts, plan slugs, checkout availability and fallback destinations.
- Lead validation, normalization, schema compatibility, duplicate handling, retry and mail fallback.
- Public-story and contribution service calls, upload contracts and family review semantics.
- Sample memory facts and approved image files, natural dimensions and responsive derivatives.
- Book reader content, keyboard behavior, focus return and reading mode. The turn animation is now a small translation/crossfade.
- Route preloading, history/anchor restoration, structured metadata, redirects and static route output.
- Legal wording and guide content. Their presentation was replaced.

Replaced:

- All main marketing pages, navigation, footer, photography composition, type hierarchy, palette roles, buttons, captions and responsive layout.
- Prior scroll-scene system and its prepaint flag.
- A-shaped website mark. Only the approved wordmark paths render in active logos and generated identity assets.
- Prior page metadata phrases and social graphics.

The active homepage imports no presentation components from either older design directory. The reusable reader and service form remain independent of those directories.
