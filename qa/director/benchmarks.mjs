import {chromium} from 'playwright';import {writeFile} from 'node:fs/promises';
const b=await chromium.launch({channel:'msedge',headless:true});
for(const [key,url] of [['binder','https://www.binder-consulting.de/'],['cargo','https://cargokite.com/'],['bear','https://bear.plus/']]) { const p=await b.newPage({viewport:{width:1440,height:900}});try{await p.goto(url,{waitUntil:'domcontentloaded',timeout:25000});await p.waitForTimeout(8000);await p.screenshot({path:`qa/director/benchmark-${key}.png`});await writeFile(`qa/director/benchmark-${key}.txt`,await p.locator('body').innerText());if(key==='bear')console.log(await p.locator('a').evaluateAll(x=>x.map(a=>({text:a.innerText,url:a.href})).filter(a=>/inder|argo|earpop|oxtaw/.test(a.text+a.url))));await p.evaluate(()=>scrollTo(0,900));await p.waitForTimeout(1500);await p.screenshot({path:`qa/director/benchmark-${key}-scroll.png`});}catch(e){console.log(key,e.message)}await p.close();}await b.close();
