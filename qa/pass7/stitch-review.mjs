import fs from 'node:fs/promises';import sharp from 'sharp';
const dir='qa/pass7',rows=JSON.parse(await fs.readFile(dir+'/tiles.json','utf8'));const meta=await sharp(dir+'/tile-0.png').metadata();const scale=meta.height/rows[0].h;const composite=[];for(const r of rows){const skip=r.i===0?0:Math.ceil(r.header*scale)+2;const buf=await sharp(dir+'/tile-'+r.i+'.png').extract({left:0,top:skip,width:meta.width,height:meta.height-skip}).toBuffer();composite.push({input:buf,left:0,top:Math.round(r.y*scale)+skip});}
await sharp({create:{width:meta.width,height:Math.ceil(Math.max(...rows.map(r=>r.y*scale+meta.height))),channels:4,background:'#F3EBDD'}}).composite(composite).png().toFile(dir+'/home-1440-full.png');await sharp(dir+'/home-1440-full.png').resize({width:216}).toFile(dir+'/home-1440-thumbnail.png');
