import fs from 'node:fs/promises';const dir='qa/greenfield';const read=async n=>JSON.parse(await fs.readFile(`${dir}/${n}.json`,'utf8'));const f=await read('functional-results'),a=await read('accessibility'),i=await read('interactive-accessibility'),r=await read('responsive-matrix');console.log(JSON.stringify({functional:f.length,failed:f.filter(x=>!x.passed),routes:r.length,overflows:r.filter(x=>x.overflow).length,a11y:a.length+i.length,violations:[...a,...i].flatMap(x=>x.violations).length}));
