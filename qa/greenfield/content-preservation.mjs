import fs from 'node:fs/promises';import assert from 'node:assert/strict';
const baseline='qa/mission-rebuild/baseline';const files=['src/lib/pricing.ts','src/lib/product.ts','src/lib/checkout.ts','src/lib/leads.ts','src/lib/supabase.ts','src/components/ui/LeadForm.tsx','src/components/publicStory.tsx','src/components/contribute.tsx','src/lib/guides.json'];const rows=[];for(const f of files){assert.equal(await fs.readFile(f,'utf8'),await fs.readFile(baseline+'/'+f,'utf8'));rows.push({file:f,unchanged:true});}for(const f of ['terms','privacy']){const before=await fs.readFile(`${baseline}/src/components/${f}.tsx`,'utf8');let after=await fs.readFile(`src/components/${f}.tsx`,'utf8');after=after.replace("'../design/a-story-greenfield/LegalLayout'","'./ui/legal'").replace("'../design/a-story-greenfield/legalPrimitives'",`'./${f}.styles'`);assert.equal(after,before);rows.push({file:`src/components/${f}.tsx`,onlyPresentationImportsChanged:true});}await fs.writeFile('qa/greenfield/content-preservation.json',JSON.stringify(rows,null,2));console.log('PASS source facts, service code, guide content and policy wording preserved.');
